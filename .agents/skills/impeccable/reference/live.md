# Live

Use browser-based visual iteration when a local dev server and browser tooling are available.

For this local install, treat live mode as a workflow instruction rather than a bundled automation.

