# Animate

Add purposeful motion.

Rules:

- Motion must clarify state, reveal content, or express brand.
- Prefer transform, opacity, filter, clip-path, or mask over layout properties.
- Use easing that feels intentional. Avoid bounce/elastic by default.
- Always include `prefers-reduced-motion`.
- Do not hide content until JavaScript runs.

